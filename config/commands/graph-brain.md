---
description: Index the current project into the codebase-memory-mcp knowledge graph and show stats
---

Index the project at the current working directory into the codebase-memory-mcp knowledge graph, then show indexed nodes, edges, and a summary of what was found. If a project name argument is provided, index that repo path instead.
