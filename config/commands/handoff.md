---
description: Perform a formal handoff of work and contracts from one sub-agent to another.
---

Execute a handoff between specialists using `team_handoff()`. Transfer state, deliverables, and shared artifacts from the origin agent (e.g. backend) to the destination agent (e.g. frontend, qa-auditor, git-flow) with context notes. Arguments: $ARGUMENTS
