---
description: Real-time multi-window swarm monitor. Shows what backend, frontend, git, and audit sessions are doing right now.
---

Display the real-time swarm activity across all active terminal windows and agent sessions by calling `team_get_live_activity()`.
Report who is active right now, which window they are operating in, their current task, relative timestamps, tasks in progress, and recent artifacts.
